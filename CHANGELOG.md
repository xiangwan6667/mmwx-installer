# 发布记录

## v0.2.11

- 打开管理菜单时自动检测脚本最新正式 Release，有新版本时提示通过菜单 9 更新。
- 每次启动只检测一次，最多等待 3 秒；查询失败不影响菜单，检测不自动下载或安装更新。
- 通过 Release 跳转查询，不占用 GitHub API 配额；按数字比较版本，忽略测试版及无效地址。

## v0.2.10

- 修复 Caddy 模块检查因管道提前关闭产生 `broken pipe`，误报缺少 Cloudflare 模块的问题。

- 菜单 4 新增日志与诊断子菜单，可查看服务日志、最近任务和失败摘要，并跟随最新任务进度。
- 新增 `mmwx trace`、`mmwx trace-follow` 和 `mmwx log-menu` 命令，便于在终端查看及跟踪任务。
- 任务日志记录操作上下文、步骤、命令名、来源行、退出码和失败摘要；Cloudflare Token、密码及 Token 文件内容写入前自动脱敏。
- 证书诊断识别 ACME 速率限制和提供商重试提示，并保留现有 ACME 账户，不设置默认邮箱。

## v0.2.9

- 完全卸载清理 Docker 镜像、网络、Docker 与 containerd 数据目录及相关软件包，并保留 `mmwx` 管理命令。
- 清理前检查其他 Docker 和 containerd 工作负载；发现其他容器、卷或非默认网络时拒绝卸载，避免误删其他业务。
- 保留数据卸载继续保留 Docker 引擎和镜像，供恢复服务使用。

## v0.2.8

- 管理脚本更新改为从 GitHub Releases 的最新稳定版本下载，不再使用主分支源码。
- 下载管理脚本及 `SHA256SUMS` 后，按同一发布版本校验 SHA-256，并确认脚本版本一致；校验失败时停止更新。
- 安装入口改为从 GitHub Releases 的 `latest` 下载，确保初次安装与脚本更新使用同一发布渠道。

## v0.2.7

- 完全卸载清理项目容器、遗留容器、定时任务、后台程序和项目防火墙规则，保留 `mmwx` 管理菜单。
- 完全卸载恢复安装前的 UFW 启用状态、规则及 IPv6 配置；恢复失败保留数据和备份供重试。
- 新安装保存各网卡 IPv6 状态及原有持久配置，兼容旧版网络备份。
- 安装开始明确提示使用无其他业务的专用服务器。
- 旧目录可直接卸载，取消操作不触发迁移或容器重启。

## v0.2.6

- 主控更新及失败恢复仅操作妙妙屋，Caddy 和 PostgreSQL 保持运行；更新完成不再触发网关重载。
- 兼容旧版中断更新，恢复已停止的网关，不重启正在运行的 Caddy。
- 卸载服务的两种模式均保留 `mmwx` 管理菜单；仅菜单 11 移除管理入口。
- 旧目录先单独迁移，再执行主控更新或回退，避免隐式迁移重启全栈。

## v0.2.5

- 新增菜单 8「Caddy 管理」，支持状态、日志、配置重载、重启、证书检查和 Cloudflare Token 替换，也可通过 `mmwx caddy` 进入。
- Token 替换先验证区域读取及临时 TXT 创建、删除权限，再仅重建 Caddy。失败恢复旧凭据，中断后通过主菜单 5 恢复。
- 分别检查本机源站与 Cloudflare 边缘证书，显示签发者、有效期及公网 HTTPS 状态。
- Caddy 写操作与维护任务、CF 定时同步互斥；凭据在日志写入前脱敏。
- 管理脚本更新、服务卸载、脚本卸载顺延为菜单 9、10、11。v0.2.4 用户从原菜单 8 更新。

妙妙屋、PostgreSQL 和现有证书均不因 Caddy 管理操作而重建或重置。旧目录需先完成迁移。

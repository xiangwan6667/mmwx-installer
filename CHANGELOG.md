# 更新日志

## v0.3.0 (2026-09-26)

本版汇总安装器现有功能与修复，将管理脚本和 Caddy 预编译依赖统一发布。

### 安装与版本管理

- 🌈 一行命令部署妙妙屋 X、PostgreSQL 18 和 Caddy，自动注册 `mmwx` 管理命令。
- 🌈 支持 Debian 12/13、Ubuntu 22.04/24.04/26.04，适配 AMD64 / ARM64；安装前检查环境，只安装缺失依赖。
- 🌈 主控可选最新正式版、最新测试版，或从各通道近期 5 个版本中指定安装；GitHub API 不可用时尝试发布网页。
- 🌈 安装前检查镜像及架构支持。最新版镜像尚未就绪时，可确认使用同通道上一可用版本；网络、限流和鉴权错误会停止操作。
- 🌈 主控更新前下载镜像并备份数据库与应用文件，失败自动恢复旧版本和备份；中断后可继续恢复。
- 🌈 手动回退支持从发布列表选择版本，仅切换主控镜像，保留当前数据。
- 🌈 独立强制重装入口，重新拉取当前镜像并重建妙妙屋，保留数据、配置、凭据和证书。
- 🛠️ fix: 主控更新、回退和重装仅操作主控，Caddy 与 PostgreSQL 保持运行；旧目录先单独迁移。
- 🛠️ fix: 已是当前版本时直接提示，避免重复下载或重启。

### Cloudflare、HTTPS 与网络

- 🌈 通过 API 选择已托管至 Cloudflare 的 Active 区域；只需输入子域名前缀，回车默认 `mmwx`。
- 🌈 自动创建开启代理的 A 记录；完全匹配的记录可复用，同名冲突不覆盖。
- 🌈 Caddy 通过 Cloudflare DNS-01 申请和续期证书，使用预编译 DNS 模块，服务器无需编译 Go。
- 🌈 UFW 放行 SSH；通过 `DOCKER-USER → MMWX-CF` 与 ipset 限制网站仅接受 Cloudflare 回源，覆盖 Docker 转发流量。
- 🌈 每日同步 Cloudflare 网段，Docker 重启时应用防护；同步失败保留原集合。
- 🌈 自动关闭系统与 UFW 的 IPv6 模式，修改前保存网络配置；五分钟内未确认新 SSH 连接时自动回滚。
- 🌈 系统及容器默认配置北京时间 `Asia/Shanghai`。

### Caddy 管理

- 🌈 独立菜单提供运行状态、日志、配置校验与重载、重启、证书检查、Token 替换和域名变更。
- 🌈 分别检查源站证书和 Cloudflare 边缘证书，显示签发者、到期时间及剩余天数，并检查公网 HTTPS。
- 🌈 Token 支持隐藏输入或 root 所有、权限为 `600` 的文件；替换前验证区域读取及临时 TXT 创建、删除权限。
- 🌈 Token 验证通过后仅重建 Caddy，使新凭据生效；失败恢复旧凭据，中断或清理失败可继续恢复，证书保持原样。
- 🌈 变更域名时自动创建新解析、申请证书并验证 HTTPS，仅重载 Caddy；失败恢复旧配置。
- 🌈 新域名生效后自动删除由脚本创建、仍指向本机且未被修改的旧代理 A 记录；清理失败可从恢复菜单重试。
- 🛠️ fix: Caddy 写操作与维护任务、Cloudflare 定时同步互斥，避免并发修改。
- 🛠️ fix: 修复模块检查管道提前关闭引起的 `broken pipe` 及 Cloudflare 模块误报。

### 数据、恢复与日志

- 🌈 配置、数据、证书、备份和运行状态分目录保存，数据库、应用文件、订阅与规则模板均持久化到宿主机。
- 🌈 PostgreSQL 连接由环境变量管理，面板初始化无需勾选「使用 PG 数据库」。
- 🌈 安装、更新、回退、重装及 Caddy 维护保存任务进度，主菜单 5 可继续任务或恢复服务。
- 🌈 新增任务追踪日志，记录时间、版本、步骤、失败位置、退出码和完整步骤输出，支持实时跟踪。
- 🌈 识别证书申请的 ACME 限流及 CA 提供的重试时间；已有 ACME 账户持续保存，不设置默认申请邮箱。
- 🛠️ fix: 已配置的 Token 和密码在终端及任务日志输出前脱敏。

### 管理脚本与卸载

- 🌈 打开菜单自动检测脚本最新正式版，最多等待 3 秒，只提示更新；检测失败不影响使用。
- 🌈 脚本安装和更新统一从正式 Release 下载，并校验同一标签的 SHA-256 与脚本版本。
- 🛠️ fix: 绕过过期的 `latest` 跳转缓存，修复刚发布后仍提示旧版的问题。
- 🛠️ fix: 更新同步管理入口；服务卸载保留 `mmwx`，管理脚本由独立菜单卸载。
- 🌈 保留数据卸载移除服务及项目网络任务，保留数据、Docker 和管理菜单，支持恢复。
- 🌈 完全卸载清理容器、镜像、Docker 网络、引擎及相关数据、定时任务和项目文件，并恢复安装前的 UFW 与 IPv6 配置。
- 🛠️ fix: 完全卸载前检查其他 Docker / containerd 工作负载，发现其他业务或网络恢复失败时停止清理。
- 📝 统一 root 执行命令与 `y/n` 确认，重写 README，保留项目图标及 MIT 许可证。
- 📦 AMD64 / ARM64 Caddy 依赖包迁入 v0.3.0，与安装脚本共用 `SHA256SUMS`；旧 Release 移除，源码标签和提交历史保留。

## 更新版本

以 root 身份执行：

```bash
mmwx self-update
mmwx --version
```

或进入 `mmwx` → **9 · 更新管理脚本**。更新后显示 `mmwx-installer 0.3.0`。

旧版安装器请先更新脚本，再安装或重建 Caddy；旧依赖包下载地址随旧 Release 一并移除。若没有 `mmwx` 命令，执行 [README 的安装命令](README.md#安装部署) 恢复管理入口。

本次更新管理脚本不会自动更新主控或重启服务。主控更新使用 **2 · 更新主控版本**；手动回退只切换镜像，旧版是否兼容当前数据库由上游决定。

## 安装与使用

完整说明见 [README](README.md)。
